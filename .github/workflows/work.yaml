name: Hello World!

on:
  push:
    branches:
      - main
      # note that your "main" branch might be called main instead of master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: data
        run: date
      - name: ls
        run: ls
               